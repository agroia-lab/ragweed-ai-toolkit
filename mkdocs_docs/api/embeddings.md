# embeddings

ResNet-50 feature extraction, MMD domain shift analysis, PCA/UMAP/t-SNE dimensionality reduction, and deployment gates.

This module implements the cross-domain evaluation pipeline from Act II. It quantifies distributional distance between training and target domains and provides actionable deployment recommendations.

::: ragweed_toolkit.embeddings
